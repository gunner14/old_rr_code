Hello World!

    <%
    /*
    String act = request.getParameter("myact");
    try{
        if(act!=null && act.equals("reload")){
            System.out.println(" reloading...");
            org.springframework.beans.factory.ListableBeanFactory context = (org.springframework.beans.factory.ListableBeanFactory)request.getAttribute("org.springframework.web.servlet.support.RequestContext.CONTEXT");
            com.renren.sns.guide.controllers.window.GuideUserCountController gc = (com.renren.sns.guide.controllers.window.GuideUserCountController)(org.springframework.beans.factory.BeanFactoryUtils.beanOfType(context, com.renren.sns.guide.controllers.window.GuideUserCountController.class));
            System.out.println(" typ:"+gc.getClass().getName());
            java.lang.reflect.Method m = gc.getClass().getDeclaredMethod("loadMap",String.class);
            m.setAccessible(true);
            m.invoke(gc,"style-");
            System.out.println(" reloaded");
            java.lang.reflect.Field fld = gc.getClass().getDeclaredField("styleMap"); 
            fld.setAccessible(true);
            java.util.Map<Integer,String> map = (java.util.Map<Integer,String>)fld.get(gc);
            for(Integer i : map.keySet()){
                System.out.println(" i:"+i+" v:"+map.get(i));
            }
            map.remove(3);
        }
    }
    catch(Exception e){
        e.printStackTrace();
    }
    */
    %>
