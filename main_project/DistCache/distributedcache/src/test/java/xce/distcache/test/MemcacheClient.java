package xce.distcache.test;

import java.io.IOException;
import java.net.InetSocketAddress;
import java.util.ArrayList;
import java.util.List;

public class MemcacheClient {

	/**
	 * @param args
	 * @throws IOException
	 */
	public static void main(String[] args) throws IOException {
		/*List<InetSocketAddress> address = new ArrayList<InetSocketAddress>();
		address.add(new InetSocketAddress("localhost", 11222));
		MemcachedClient client = new MemcachedClient(new BinaryConnectionFactory(), address);
		Object x = client.get("(null,null)");
		System.out.println(x);*/
	}
}
